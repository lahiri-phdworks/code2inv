int main()
{
    long long int z1, z2, z3;
    long long int x;
    long long int m;
    long long int n;

    // pre-conditions
    // scanf("%d", &x);
    // scanf("%d", &m);
    // scanf("%d", &n);
    (x = 0);
    (m = 0);
    // precheck
    // loopcond : (x < n)
    // loopstart
    while (x < n)
    {
        // loop body
        // long long int temp_x = x;
        // long long int temp_m = m;
        // long long int temp_n = n;
        if (unknown())
        {
            m = x;
        }
        x += 1;
    }
    // loopend
    // postcheck
    // post-condition
    if (n > 0)
    {
        // assert (m < n);
        assert(m >= 0);
    }
}
