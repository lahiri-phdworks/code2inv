#define PHI                                                                    \
  (((arr[mid] != item) && (mid == mid) && (key != mid)) ||                     \
   (arr[mid] == item)) // generated by code2inv
