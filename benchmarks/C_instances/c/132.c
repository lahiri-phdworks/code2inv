int main() {
  // variable declarations
  int i = 0;
  int j, c, t;
  // pre-conditions
  // scanf("%d", &i);
  // scanf("%d", &j);
  // scanf("%d", &c);
  // precheck
  // loopcond : (unknown())
  // loopstart
  while (unknown()) {
    if (c > 48) {
      if (c < 57) {
        j = i + i;
        t = c - 48;
        i = j + t;
      }
    }
  }
  // loopend
  // postcheck
  // post-condition
  assert(i >= 0);
}
