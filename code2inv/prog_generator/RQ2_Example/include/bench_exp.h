#define PHI                                                                    \
  ((a >= 1) && (b >= 0) &&                                                     \
   (res * pow(a, b) == pow(x, y))) // generated by code2inv
